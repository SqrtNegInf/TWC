 
===> 255/2/athanasius.pl <===

Challenge 255, Task #2: Most Frequent Word (Perl)

Running the test suite
ok 1 - Example 1
ok 2 - Example 2
ok 3 - Gettysburg
ok 4 - Case 1
ok 5 - Case 2
1..5
 
===> 255/2/bob-lied.pl <===
# Seeded srand with seed '1' from import arg.
ok 1 - Example 1
ok 2 - Example 2
1..2
 
===> 255/2/cheok-yin-fung.pl <===
# Seeded srand with seed '1' from import arg.
ok 1
ok 2
1..2
 
===> 255/2/dave-jacoby.pl <===
Input:  $p = "Joe hit a ball, the hit ball flew far after it was hit."
        $w = "hit"
Output: "ball"

Input:  $p = "Perl and Raku belong to the same family. Perl is the most popular language in the weekly challenge."
        $w = "the"
Output: "Perl"

 
===> 255/2/deadmarshal.pl <===
ball
Perl
 
===> 255/2/e-choroba.pl <===
# Seeded srand with seed '1' from import arg.
1..3
ok 1 - Example 1
ok 2 - Example 2
ok 3 - More than one
 
===> 255/2/jo-37.pl <===
# Seeded srand with seed '1' from import arg.
ok 1 - example 1
ok 2 - example 2
ok 3 - more banned words
1..3
 
===> 255/2/kjetillll.pl <===
ok 1
ok 2
1..2
 
===> 255/2/laurent-rosenfeld.pl <===
Joe hit a ball, the hit ball  ... => ball
Perl and Raku belong to the   ... => Perl
 
===> 255/2/lubos-kolouch.pl <===
ball
perl
 
===> 255/2/nelo-tovar.pl <===
Input : $p = Joe hit a ball, the hit ball flew far after it was hit.
        $w = hit
Output : ball
 
Input : $p = Perl and Raku belong to the same family. Perl is the most popular language in the weekly challenge.
        $w = the
Output : Perl
 
 
===> 255/2/packy-anderson.pl <===
Example 1:
Input: $p = "Joe hit a ball, the hit ball flew far after it was hit."
       $w = "hit"
Output: "ball"

The banned word "hit" occurs 3 times.
The other word "ball" occurs 2 times.

Example 2:
Input: $p = "Perl and Raku belong to the same family. Perl is the most popular language in the weekly challenge."
       $w = "the"
Output: "perl"

The banned word "the" occurs 3 times.
The other word "perl" occurs 2 times.
 
===> 255/2/perlboy1967.pl <===
# Seeded srand with seed '1' from import arg.
ok 1
ok 2
1..2
 
===> 255/2/peter-campbell-smith.pl <===

Input:  $p = 'Joe hit a ball, the hit ball flew far 
	after it was hit by Joe.'
        $w = 'hit'
Output: 'ball, joe' - 2 times
        'hit' (banned) - 3 times

Input:  $p = 'Perl and Raku belong to the same 
	family. Perl is the most popular language in the 
	weekly challenge.'
        $w = 'the'
Output: 'perl' - 2 times
        'the' (banned) - 3 times

Input:  $p = 'banned banned allowed allowed allowed'
        $w = 'banned'
Output: 'allowed' - 3 times
        'banned' (banned) - 2 times
 
===> 255/2/peter-meszaros.pl <===
# Seeded srand with seed '1' from import arg.
ok 1 - Example 1
ok 2 - Example 2
1..2
 
===> 255/2/roger-bell-west.pl <===
1..2
ok 1 - example 1
ok 2 - example 2
 
===> 255/2/spadacciniweb.pl <===
Joe hit a ball the hit ball flew far after it was hit | hit -> ball
Perl and Raku belong to the same family Perl is the most popular language in the weekly challenge | the -> Perl
 
===> 255/2/wanderdoc.pl <===
# Seeded srand with seed '1' from import arg.
ok 1 - Example 1
ok 2 - Example 2
1..2
 
===> 255/2/zapwai.pl <===
Input: $p = Joe hit a ball, the hit ball flew far after it was hit.
	$w = hit
Output: ball

Input: $p = Perl and Raku belong to the same family. Perl is the most popular language in the weekly challenge.
	$w = the
Output: Perl

